# .files
symlinked dotfiles

ln -s (1) (2)
ln -s $HOME/.config/.files/.zshrc $HOME/.config/zsh/.zshrc
ln -s $HOME/.config/.files/.vimrc $HOME/.vimrc
ln -s $HOME/.config/.files/.zshenv $HOME/.zshenv
ln -s $HOME/.config/.files/starship.toml $HOME/.config/starship.toml

--symlink script--?
